{% extends 'base.html.twig' %}

{% block title %}Hello CategoriaController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp64/www/SYMFONY_2DAW/2daw/src/Controller/CategoriaController.php'|file_link(0) }}">src/Controller/CategoriaController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp64/www/SYMFONY_2DAW/2daw/templates/categoria/index.html.twig'|file_link(0) }}">templates/categoria/index.html.twig</a></code></li>
    </ul>
</div>

{% for message in app.session.flashbag().get('message') %}
    <h1>{{ message }}</h1>
{% endfor %}

<a href="{{ path('categoria_create') }}">Crear categoria</a>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Tipo</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        {% for u in categorias %}
        <tr>
            <td>{{ u.id }}</td>
            <td>{{ u.nombre }}</td>
            <td>{{ u.tipo }}</td>
            <td> 
                <a href="{{ path('categoria_edit', {'id': u.id}) }}">Editar</a>
                <a href="{{ path('categoria_destroy', {'id': u.id}) }}">Eliminar</a>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
